1.BUGS
-some sub menus dont enable `g` for global return.
	'g' needs to be set as a global toogle just like 'q' for quiting


2.CRASHES
-entering midi options crashes the application back to the shell

3.INCOMPLETE FEATURES
-the bpm counter is still to instable an inaccurate we need at at least 80% accuricy at all times.
	how to achieve this?

4.ADVANCING EXISTING FEATURES
- hitting 'q' should ask the user if they are sure to exit the application, not instantly exit.

5.ADDITONAL FEATURES 
-we need video output handling and the controlling  ui should be in a seperat window where we can use the whole screen for value display for visual clearance whats going on.
	and to keep the output part clear of ui elements.
-

6.UI/UX
-

s
